class BatchData(object):
    """docstring for Pipeline"""
    def __init__(self, file_name, size, batch_num):
        super(Pipeline, self).__init__()
        self.arg = arg

    def sample():
        for data in datas:
            return batch

        